'use client';

export default function Session2() {
  //colocar dados em uma const por exemplo, um array com objeto e chaves title, gif, description, botão opcional, tecnologias utilizadas, etc.

  return (
    <section className="min-h-screen bg-red-100" id="professional-projects">
      sessão 2
    </section>
  );
}
